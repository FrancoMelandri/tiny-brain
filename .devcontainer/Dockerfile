# Use the official .NET 8 SDK image
FROM mcr.microsoft.com/dotnet/sdk:8.0

# Set the working directory inside the container
WORKDIR /workspace

# (Optional) Install additional tools or dependencies if needed
# RUN apt-get update && apt-get install -y <your-dependency>

# Expose ports if your application requires them
# Example: EXPOSE 5000

# Define default entry point if applicable
CMD [ "sleep", "infinity" ]
